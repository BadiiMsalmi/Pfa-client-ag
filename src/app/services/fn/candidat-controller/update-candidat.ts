/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Candidat } from '../../models/candidat';

export interface UpdateCandidat$Params {
  id: number;
      body: Candidat
}

export function updateCandidat(http: HttpClient, rootUrl: string, params: UpdateCandidat$Params, context?: HttpContext): Observable<StrictHttpResponse<Candidat>> {
  const rb = new RequestBuilder(rootUrl, updateCandidat.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Candidat>;
    })
  );
}

updateCandidat.PATH = '/api/v1/candidats/update/{id}';
